# E-SCHEMA: visibility vocabulary is project-defined, but the slot must still be
# a non-empty string; an empty value is rejected.
visibility: ""
description:
  1:
    text: The widget shall do the thing.
    parent_req:
      - hlr_Widget_1.1
