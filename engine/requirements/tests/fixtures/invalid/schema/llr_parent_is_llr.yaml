# E-PARENT-TYPE: parent_req must reference an HLR statement; here it resolves to
# a statement in an LLR (llr_target_1.1) when validated alongside it.
visibility: compiler
description:
  1:
    text: The widget shall do the dependent thing.
    parent_req:
      - llr_target_1.1
