# E-DESCKEY-DUP: statement number 1 is repeated. safe_load would silently merge
# the two (last wins); the duplicate is caught from the raw YAML node tree.
description:
  1:
    text: The widget shall do the first thing.
    derived: true
  1:
    text: The widget shall do the first thing differently.
    derived: true
